class ActionProperties
def actions
    return <%= p('persistent_volume.pvs') %>
end
def storageclass
  var result = "<%= p('persistent_volume.storageclass.name') %>"
  puts "Storageclass: #{result}"
  return result
end
def create_storageclass
  return <%= p('persistent_volume.storageclass.create') %>
end
end