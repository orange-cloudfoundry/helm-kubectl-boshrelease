#!/usr/bin/env ruby
# encoding: utf-8

require 'yaml'

NAMESPACE_YAML= <%= p('namespaces') %>
MAX_RETRIES = 3
SLEEP_DURATION = 5
failCmd=[]


data = NAMESPACE_YAML

data.each { |namespace|
    cmd = "kubectl delete ns #{namespace} "
    result=system("#{cmd} > /dev/null 2>&1")
 }



