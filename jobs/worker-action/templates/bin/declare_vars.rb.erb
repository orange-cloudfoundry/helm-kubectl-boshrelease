class ActionProperties
def self.actions
    return <%= p('persistent_volume.pvs') %>
end
def self.based_on_label
  result = "<%= p('persistent_volume.based_on_label') %>"
  puts "based_on_label: #{result}"
  return result
end

def self.storageclass
  result = "<%= p('persistent_volume.storageclass.name') %>"
  puts "Storageclass: #{result}"
  return result
end

def self.create_storageclass
  return "<%= p('persistent_volume.storageclass.create') %>"
end
end